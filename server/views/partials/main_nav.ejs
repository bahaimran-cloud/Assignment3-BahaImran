<nav class="navbar bg-secondary navbar-expand-lg" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Assessment Tracker</a>
    <!-- Toggler/collapsibe Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!-- Navbar links -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <!-- Home link -->
        <li class="nav-item">
          <% if (title == 'Assessment Tracker - Home Page' || title == 'Home Page') { %>
            <a class="nav-link active" aria-current="page" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
          <% } else { %>
            <a class="nav-link" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
          <% } %>
        </li>
        <!-- Contact Me link -->
        <li class="nav-item">
          <% if (title == 'Assessment Tracker - Contact Me' ) { %>
            <a class="nav-link active" aria-current="page" href="/contact"><i class="fa-solid fa-lg fa-address-card"></i> Contact Me</a>
          <% } else { %>
            <a class="nav-link" href="/contact"><i class="fa-solid fa-lg fa-address-card"></i> Contact Me</a>
          <% } %>
        </li>
        <!-- Assessments link -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid fa-lg fa-tasks"></i> Assessments
          </a>
          <ul class="dropdown-menu">
            <!-- View All and Add Assessment links -->
            <li><a class="dropdown-item" href="/assessments"><i class="fa-solid fa-list"></i> View All</a></li>
            <!--add Assessment link -->
            <% if (user) { %>
              <li><a class="dropdown-item" href="/assessments/add"><i class="fa-solid fa-plus"></i> Add Assessment</a></li>
            <% } else { %>
              <li><span class="dropdown-item text-muted"><i class="fa-solid fa-lock"></i> Login to add</span></li>
            <% } %>
          </ul>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if (user) { %>
          <li class="nav-item">
            <span class="navbar-text me-3"><i class="fa-regular fa-user"></i> <%= user.displayName || user.username %></span>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/auth/logout"><i class="fa-solid fa-right-from-bracket"></i> Logout</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/auth/login"><i class="fa-solid fa-right-to-bracket"></i> Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/auth/register"><i class="fa-solid fa-user-plus"></i> Register</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
